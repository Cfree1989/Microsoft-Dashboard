# Unpacked Power Apps source

YAML/JSON here is produced by [`../Unpack-Apps.ps1`](../Unpack-Apps.ps1) (`pac canvas unpack`). To push repo edits back into an app file, run [`../Pack-Apps.ps1`](../Pack-Apps.ps1) (`pac canvas pack`), then open the resulting `.msapp` in Power Apps Studio, save to the cloud, and publish.
