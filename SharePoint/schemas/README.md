# SharePoint list schemas (exported)

**If you do not have IT consent for an Entra app:** you do not need this folder. Use unpacked Power Apps under [`PowerApps/source/.../pkgs/TableDefinitions/`](../../PowerApps/source/StaffDashboard/pkgs/TableDefinitions/) and/or paste JSON from a one-off **Send an HTTP request to SharePoint** flow — see [`EXPORTS.md`](../../EXPORTS.md).

Optional: JSON here can be generated by [`../Export-Schemas.ps1`](../Export-Schemas.ps1) (**PowerShell 7** + consented `-ClientId` where the tenant blocks the default PnP app).
