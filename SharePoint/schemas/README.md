# SharePoint list schemas (exported)

JSON files here are generated by [`../Export-Schemas.ps1`](../Export-Schemas.ps1). Re-run that script after you change columns in SharePoint so this repo stays in sync with the live site.
